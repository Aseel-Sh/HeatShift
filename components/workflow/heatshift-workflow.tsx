"use client";

import { useEffect, useReducer } from "react";
import { AlertTriangle, ArrowLeft, ArrowRight, Check, CloudSun, LoaderCircle, Plus, ShieldCheck, Sparkles, Trash2 } from "lucide-react";
import { BrandHeader } from "@/components/layout/brand-header";
import { getDemoScenario } from "@/lib/demo/get-demo-scenario";
import type { ExtractedPlan } from "@/lib/ai/plan-extraction-schema";
import { generateSchedule } from "@/lib/domain/scheduler";
import { classifyForecastTemperature } from "@/lib/domain/temperature-category";
import type { ForecastHour, SaudiCity, WorkTask } from "@/lib/domain/types";
import { createInitialWorkflowState, validatePlanDetails, validateVerifiedPlan, workflowReducer, type PlanForm, type WorkflowState } from "@/lib/workflow/state";

const text = {
  en: { describe:"Describe plan", verify:"Verify tasks", conditions:"Select conditions", result:"Safer shift", title:"Plan a safer outdoor shift", subtitle:"Describe one crew's day, verify every task, then select the conditions used for planning guidance.", site:"Site name", city:"City", date:"Shift date", start:"Shift start", end:"Shift end", crew:"Crew size", newWorkers:"Non-acclimatized workers", plan:"Natural-language work plan", hint:"Include tasks, durations, workload, and where the work happens. AI extracts structure; it does not make safety decisions.", analyze:"Analyze plan", analyzing:"Analyzing plan", demo:"Load demo scenario", manual:"Create tasks manually", verifyTitle:"Verify and edit every task", notice:"AI extracted the plan. Verify every field before scheduling.", add:"Add task", back:"Back", next:"Continue to conditions", enName:"English name", arName:"Arabic name", duration:"Duration (minutes)", workload:"Workload", environment:"Environment", split:"Task may be split", remove:"Delete task", light:"Light", heavy:"Heavy", direct_sun:"Direct sun", shaded_outdoor:"Shaded outdoor", indoor:"Indoor", assumptions:"Assumptions", missing:"Missing information", conditionsTitle:"Select planning conditions", loading:"Loading hourly forecast…", peak:"Peak temperature", risk:"Forecast temperature-risk category", unavailable:"Forecast unavailable", fallback:"You can continue with preliminary planning or enter a site-verified TWL reading. No weather values were invented.", forecast:"Forecast only", forecastCopy:"Preliminary forecast plan. Exact TWL recovery cycles will not be claimed.", onsite:"On-site TWL reading", twlExplain:"Enter the zone from an appropriate on-site assessment. TWL is not calculated from the ordinary forecast.", low:"Low: 140 to less than 220", intermediate:"Intermediate: 115 to less than 140", high:"High risk: below 115", generate:"Generate safer shift", generated:"Basic schedule generated", generatedCopy:"The deterministic scheduler completed. The polished results and report screen remains deferred.", guidance:"Planning guidance only. Verify conditions using qualified on-site safety procedures.", task:"Task", demoLabel:"Deterministic demo data", select:"Select" },
  ar: { describe:"وصف الخطة", verify:"مراجعة المهام", conditions:"اختيار الظروف", result:"وردية أكثر أمانًا", title:"خطط لوردية خارجية أكثر أمانًا", subtitle:"صِف يوم فريق واحد، وراجع كل مهمة، ثم اختر الظروف المستخدمة في إرشادات التخطيط.", site:"اسم الموقع", city:"المدينة", date:"تاريخ الوردية", start:"بداية الوردية", end:"نهاية الوردية", crew:"حجم الفريق", newWorkers:"العمال غير المتأقلمين", plan:"خطة العمل باللغة الطبيعية", hint:"أدرج المهام والمدد وعبء العمل ومكان التنفيذ. يستخرج الذكاء الاصطناعي البيانات ولا يتخذ قرارات السلامة.", analyze:"تحليل الخطة", analyzing:"جارٍ تحليل الخطة", demo:"تحميل السيناريو التجريبي", manual:"إنشاء المهام يدويًا", verifyTitle:"راجع وعدّل كل مهمة", notice:"استخرج الذكاء الاصطناعي الخطة. تحقق من كل حقل قبل الجدولة.", add:"إضافة مهمة", back:"رجوع", next:"متابعة إلى الظروف", enName:"الاسم الإنجليزي", arName:"الاسم العربي", duration:"المدة (بالدقائق)", workload:"عبء العمل", environment:"بيئة العمل", split:"يمكن تقسيم المهمة", remove:"حذف المهمة", light:"خفيف", heavy:"ثقيل", direct_sun:"شمس مباشرة", shaded_outdoor:"خارجي مظلل", indoor:"داخلي", assumptions:"الافتراضات", missing:"المعلومات الناقصة", conditionsTitle:"اختر ظروف التخطيط", loading:"جارٍ تحميل التوقعات لكل ساعة…", peak:"درجة الحرارة القصوى", risk:"فئة مخاطر حرارة التوقعات", unavailable:"التوقعات غير متاحة", fallback:"يمكنك المتابعة بتخطيط أولي أو إدخال قراءة TWL موثقة من الموقع. لم يتم اختلاق بيانات طقس.", forecast:"التوقعات فقط", forecastCopy:"خطة توقعات أولية. لن يتم الادعاء بتطبيق دورات تعافٍ دقيقة لـ TWL.", onsite:"قراءة TWL من الموقع", twlExplain:"أدخل النطاق من تقييم موقعي مناسب. لا يُحسب TWL من توقعات الطقس العادية.", low:"منخفض: من 140 إلى أقل من 220", intermediate:"متوسط: من 115 إلى أقل من 140", high:"خطر مرتفع: أقل من 115", generate:"إنشاء وردية أكثر أمانًا", generated:"تم إنشاء الجدول الأساسي", generatedCopy:"اكتملت الجدولة الحتمية. لا تزال شاشة النتائج والتقرير المصقولة مؤجلة.", guidance:"إرشادات تخطيط فقط. تحقق من الظروف باستخدام إجراءات سلامة ميدانية مؤهلة.", task:"مهمة", demoLabel:"بيانات تجريبية حتمية", select:"اختر" },
} as const;
type T = typeof text.en | typeof text.ar;
const cities: SaudiCity[] = ["riyadh","jeddah","dammam","mecca","medina"];
const errorMessage = (errors: Record<string,string>, key: string) => errors[key] ? <p id={`${key}-error`} role="alert" className="mt-1 text-sm font-semibold text-red-700">{errors[key]}</p> : null;

export function HeatShiftWorkflow() {
  const [state, dispatch] = useReducer(workflowReducer, undefined, createInitialWorkflowState);
  const t = text[state.language];
  useEffect(() => { document.documentElement.lang=state.language; document.documentElement.dir=state.language==="ar"?"rtl":"ltr"; }, [state.language]);
  const setField=(field:keyof PlanForm,value:string)=>dispatch({type:"setPlanField",field,value});
  async function analyze() {
    const errors=validatePlanDetails(state.plan); if(state.plan.planText.trim().length<10) errors.planText="Enter at least 10 meaningful characters.";
    if(Object.keys(errors).length) return dispatch({type:"setErrors",errors});
    dispatch({type:"setAiStatus",status:"loading"});
    try { const response=await fetch("/api/parse-plan",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({text:state.plan.planText})}); const payload=await response.json() as {data?:ExtractedPlan;error?:{message?:string;code?:string}}; if(!response.ok||!payload.data) throw new Error(payload.error?.message||payload.error?.code||"AI extraction is unavailable."); dispatch({type:"applyExtraction",extraction:payload.data}); }
    catch(error){dispatch({type:"setAiStatus",status:"error",error:error instanceof Error?error.message:"AI extraction is unavailable."});}
  }
  function manual(){const errors=validatePlanDetails(state.plan);if(Object.keys(errors).length)return dispatch({type:"setErrors",errors});dispatch({type:"setStep",step:"verify"});}
  async function conditions(){const errors=validateVerifiedPlan(state.plan,state.tasks);if(Object.keys(errors).length)return dispatch({type:"setErrors",errors});dispatch({type:"setStep",step:"conditions"});if(state.isDemo)return;dispatch({type:"weatherLoading"});try{const response=await fetch(`/api/weather?city=${state.plan.city}&date=${state.plan.shiftDate}`);const payload=await response.json() as {data?:{hours?:ForecastHour[]};error?:{message?:string}};if(!response.ok||!payload.data?.hours?.length)throw new Error(payload.error?.message||"Weather is unavailable.");dispatch({type:"weatherSuccess",forecast:payload.data.hours});}catch(error){dispatch({type:"weatherError",error:error instanceof Error?error.message:"Weather is unavailable."});}}
  function generate(){dispatch({type:"setScheduleResult",result:generateSchedule({siteName:state.plan.siteName,city:state.plan.city as SaudiCity,shiftDate:state.plan.shiftDate,shiftStart:state.plan.shiftStart,shiftEnd:state.plan.shiftEnd,crewSize:Number(state.plan.crewSize),nonAcclimatizedWorkers:Number(state.plan.nonAcclimatizedWorkers),tasks:state.tasks},state.conditions,state.forecast)});}
  const steps=[t.describe,t.verify,t.conditions,t.result];const active=state.step==="describe"?0:state.step==="verify"?1:state.scheduleResult?3:2;const peak=state.forecast.length?Math.max(...state.forecast.map(h=>h.temperatureCelsius)):null;
  return <main id="main-content" className="min-h-screen bg-[var(--sand-50)] text-[var(--navy-950)]"><BrandHeader language={state.language} onLanguageChange={language=>dispatch({type:"setLanguage",language})}/><div className="mx-auto max-w-6xl px-4 py-8 sm:px-8 lg:px-10"><header className="max-w-3xl"><p className="inline-flex items-center gap-2 rounded-full border border-amber-300 bg-amber-50 px-3 py-1.5 text-sm font-bold"><ShieldCheck className="size-4"/>HeatShift — <span lang="ar">وردية آمنة</span></p><h1 className="mt-5 text-3xl font-bold tracking-tight sm:text-5xl">{t.title}</h1><p className="mt-4 text-lg leading-8 text-[var(--navy-700)]">{t.subtitle}</p></header>
    <nav aria-label="Planning progress" className="mt-8"><ol className="grid grid-cols-2 gap-2 lg:grid-cols-4">{steps.map((label,i)=><li key={label} aria-current={i===active?"step":undefined} className={`flex items-center gap-3 rounded-xl border px-3 py-3 text-sm font-bold ${i===active?"border-amber-500 bg-amber-50":"border-[var(--sand-300)] bg-white"}`}><span className={`flex size-7 items-center justify-center rounded-full ${i<=active?"bg-[var(--navy-950)] text-white":"bg-[var(--sand-100)]"}`}>{i<active?<Check className="size-4"/>:i+1}</span>{label}</li>)}</ol></nav>
    <section className="mt-6 rounded-2xl border border-[var(--sand-300)] bg-white p-5 shadow-[0_16px_45px_rgba(16,37,55,0.07)] sm:p-8">{state.step==="describe"?<Describe state={state} t={t} setField={setField} analyze={analyze} manual={manual} demo={()=>dispatch({type:"loadDemo",demo:getDemoScenario()})}/>:state.step==="verify"?<Verify state={state} t={t} dispatch={dispatch} next={conditions}/>:<Conditions state={state} t={t} peak={peak} dispatch={dispatch} generate={generate}/>}</section>
    <footer className="mt-6 flex gap-3 border-t border-[var(--sand-300)] py-6 text-sm text-[var(--navy-700)]"><ShieldCheck className="size-5 shrink-0 text-amber-700"/>{t.guidance}</footer></div></main>;
}

function Describe({state,t,setField,analyze,manual,demo}:{state:WorkflowState;t:T;setField:(f:keyof PlanForm,v:string)=>void;analyze:()=>void;manual:()=>void;demo:()=>void}){
 const fields:[keyof PlanForm,string,string][]=[["siteName",t.site,"text"],["shiftDate",t.date,"date"],["shiftStart",t.start,"time"],["shiftEnd",t.end,"time"],["crewSize",t.crew,"number"],["nonAcclimatizedWorkers",t.newWorkers,"number"]];
 return <><h2 className="text-2xl font-bold">{t.describe}</h2><div className="mt-6 grid gap-5 sm:grid-cols-2"><label><span className="form-label">{t.city}</span><select className="form-control" value={state.plan.city} onChange={e=>setField("city",e.target.value)} aria-describedby={state.errors.city?"city-error":undefined}><option value="">{t.select}</option>{cities.map(c=><option key={c} value={c}>{c[0].toUpperCase()+c.slice(1)}</option>)}</select>{errorMessage(state.errors,"city")}</label>{fields.map(([field,label,type])=><label key={field}><span className="form-label">{label}</span><input className="form-control" dir={type==="text"?undefined:"ltr"} type={type} step={type==="time"?300:undefined} min={type==="number"?field==="crewSize"?1:0:undefined} value={state.plan[field]} onChange={e=>setField(field,e.target.value)} aria-describedby={state.errors[field]?`${field}-error`:undefined}/>{errorMessage(state.errors,field)}</label>)}</div><label className="mt-5 block"><span className="form-label">{t.plan}</span><textarea className="form-control resize-y" rows={6} maxLength={5000} value={state.plan.planText} onChange={e=>setField("planText",e.target.value)} aria-describedby="plan-hint"/><span id="plan-hint" className="mt-2 block text-sm text-[var(--navy-600)]">{t.hint}</span>{errorMessage(state.errors,"planText")}</label>{state.aiError&&<div role="alert" className="mt-5 rounded-lg border border-red-300 bg-red-50 p-4 text-red-900"><strong>AI:</strong> {state.aiError}<span className="mt-1 block">{t.manual}</span></div>}<div className="mt-6 flex flex-wrap gap-3"><button className="button-primary" onClick={analyze} disabled={state.aiStatus==="loading"}>{state.aiStatus==="loading"?<><LoaderCircle className="size-4 animate-spin"/>{t.analyzing}</>:<><Sparkles className="size-4"/>{t.analyze}</>}</button><button className="button-secondary" onClick={demo}>{t.demo}</button><button className="button-quiet" onClick={manual}>{t.manual}</button></div></>;
}

function Verify({state,t,dispatch,next}:{state:WorkflowState;t:T;dispatch:React.Dispatch<Parameters<typeof workflowReducer>[1]>;next:()=>void}){
 return <><div className="flex flex-wrap justify-between gap-4"><div><h2 className="text-2xl font-bold">{t.verifyTitle}</h2><p className="mt-2 text-[var(--navy-700)]">{t.notice}</p>{state.isDemo&&<span className="mt-3 inline-block rounded-full bg-amber-100 px-3 py-1 text-xs font-bold">{t.demoLabel}</span>}</div><button className="button-secondary" onClick={()=>dispatch({type:"addTask"})}><Plus className="size-4"/>{t.add}</button></div>{state.assumptions.length>0&&<Info title={t.assumptions} items={state.assumptions}/>} {state.missingInformation.length>0&&<Info title={t.missing} items={state.missingInformation}/>} {errorMessage(state.errors,"tasks")}<div className="mt-6 space-y-4">{state.tasks.map((task,i)=><Task task={task} index={i} key={task.id} t={t} errors={state.errors} dispatch={dispatch}/>)}</div><div className="mt-7 flex justify-between gap-3"><button className="button-quiet" onClick={()=>dispatch({type:"setStep",step:"describe"})}><ArrowLeft className="size-4"/>{t.back}</button><button className="button-primary" onClick={next}>{t.next}<ArrowRight className="size-4"/></button></div></>;
}
function Info({title,items}:{title:string;items:string[]}){return <div className="mt-4 rounded-lg bg-[var(--sand-100)] p-4"><h3 className="font-bold">{title}</h3><ul className="mt-2 list-disc px-5 text-sm">{items.map(x=><li key={x}>{x}</li>)}</ul></div>}
function Task({task,index,t,errors,dispatch}:{task:WorkTask;index:number;t:T;errors:Record<string,string>;dispatch:React.Dispatch<Parameters<typeof workflowReducer>[1]>}){
 const update=(field:keyof Omit<WorkTask,"id">,value:WorkTask[keyof Omit<WorkTask,"id">])=>dispatch({type:"updateTask",id:task.id,field,value});const err=(f:string)=>errors[`task-${task.id}-${f}`];
 return <article data-testid={`task-card-${index}`} className="rounded-xl border border-[var(--sand-300)] bg-[var(--sand-50)] p-4"><div className="mb-4 flex justify-between"><h3 className="font-bold">{t.task} {index+1}</h3><button className="button-danger" aria-label={`${t.remove} ${index+1}`} onClick={()=>dispatch({type:"removeTask",id:task.id})}><Trash2 className="size-4"/>{t.remove}</button></div><div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-3"><label><span className="form-label">{t.enName}</span><input className="form-control" value={task.nameEn} onChange={e=>update("nameEn",e.target.value)}/>{err("nameEn")&&<p className="text-sm text-red-700">{err("nameEn")}</p>}</label><label><span className="form-label">{t.arName}</span><input className="form-control" lang="ar" dir="rtl" value={task.nameAr} onChange={e=>update("nameAr",e.target.value)}/>{err("nameAr")&&<p className="text-sm text-red-700">{err("nameAr")}</p>}</label><label><span className="form-label">{t.duration}</span><input className="form-control" type="number" min="5" step="5" dir="ltr" value={task.durationMinutes} onChange={e=>update("durationMinutes",Number(e.target.value))}/>{err("durationMinutes")&&<p className="text-sm text-red-700">{err("durationMinutes")}</p>}</label><label><span className="form-label">{t.workload}</span><select className="form-control" value={task.workload} onChange={e=>update("workload",e.target.value as WorkTask["workload"])}><option value="light">{t.light}</option><option value="heavy">{t.heavy}</option></select></label><label><span className="form-label">{t.environment}</span><select className="form-control" value={task.environment} onChange={e=>update("environment",e.target.value as WorkTask["environment"])}><option value="direct_sun">{t.direct_sun}</option><option value="shaded_outdoor">{t.shaded_outdoor}</option><option value="indoor">{t.indoor}</option></select></label><label className="flex items-center gap-3 self-end rounded-lg border bg-white p-3"><input className="size-5" type="checkbox" checked={task.splittable} onChange={e=>update("splittable",e.target.checked)}/><span className="font-semibold">{t.split}</span></label></div></article>;
}

function Conditions({state,t,peak,dispatch,generate}:{state:WorkflowState;t:T;peak:number|null;dispatch:React.Dispatch<Parameters<typeof workflowReducer>[1]>;generate:()=>void}){
 const onsite=state.conditions.measurementMode==="onsite_twl";return <><h2 className="text-2xl font-bold">{t.conditionsTitle}</h2><div className="mt-6 rounded-xl border border-[var(--sand-300)] bg-[var(--sand-50)] p-4">{state.weatherStatus==="loading"&&<p role="status" className="flex gap-2"><LoaderCircle className="size-5 animate-spin"/>{t.loading}</p>}{state.weatherStatus==="error"&&<div role="alert"><p className="font-bold text-red-800"><AlertTriangle className="me-2 inline size-5"/>{t.unavailable}</p><p className="mt-2 text-sm">{state.weatherError}</p><p className="mt-2 text-sm font-semibold">{t.fallback}</p></div>}{state.weatherStatus==="success"&&<><div className="flex gap-8"><p><span className="block text-sm">{t.peak}</span><strong className="text-2xl" dir="ltr">{peak?.toFixed(1)}°C</strong></p><p><span className="block text-sm">{t.risk}</span><strong>{peak===null?"—":classifyForecastTemperature(peak).category.replace("_"," ")}</strong></p></div><div aria-label="Hourly forecast" className="mt-4 flex gap-2 overflow-x-auto">{state.forecast.filter(h=>h.time>=state.plan.shiftStart&&h.time<=state.plan.shiftEnd).map(h=><div key={h.time} className="min-w-24 rounded-lg border bg-white p-3 text-center" dir="ltr"><span>{h.time}</span><strong className="block">{h.temperatureCelsius.toFixed(1)}°</strong></div>)}</div></>}</div><fieldset className="mt-6 space-y-3"><legend className="sr-only">Measurement mode</legend><label className="block rounded-xl border p-4"><span className="flex gap-3"><input type="radio" name="mode" checked={!onsite} onChange={()=>dispatch({type:"setConditions",conditions:{measurementMode:"forecast",twlZone:"none"}})}/><strong>{t.forecast}</strong></span><span className="mt-2 block text-sm">{t.forecastCopy}</span></label><label className="block rounded-xl border p-4"><span className="flex gap-3"><input type="radio" name="mode" checked={onsite} onChange={()=>dispatch({type:"setConditions",conditions:{measurementMode:"onsite_twl",twlZone:"low"}})}/><strong>{t.onsite}</strong></span><span className="mt-2 block text-sm">{t.twlExplain}</span></label></fieldset>{onsite&&<fieldset className="mt-4 grid gap-3 sm:grid-cols-3"><legend className="mb-2 font-bold">TWL</legend>{(["low","intermediate","high"] as const).map(z=><label key={z} className="flex gap-3 rounded-lg border p-3"><input type="radio" name="twl" checked={state.conditions.twlZone===z} onChange={()=>dispatch({type:"setConditions",conditions:{measurementMode:"onsite_twl",twlZone:z}})}/>{t[z]}</label>)}</fieldset>}{state.scheduleResult&&<div role="status" className="mt-6 rounded-xl border border-emerald-300 bg-emerald-50 p-5"><h3 className="font-bold">{t.generated}</h3><p className="mt-2 text-sm">{t.generatedCopy}</p><p className="mt-3 font-semibold" dir="ltr">{state.scheduleResult.metrics.scheduledWorkMinutes} min work · {state.scheduleResult.metrics.unscheduledMinutes} min unscheduled</p></div>}<div className="mt-7 flex justify-between"><button className="button-quiet" onClick={()=>dispatch({type:"setStep",step:"verify"})}><ArrowLeft className="size-4"/>{t.back}</button><button className="button-primary" onClick={generate}><CloudSun className="size-4"/>{t.generate}</button></div></>;
}
