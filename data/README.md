# Data fixtures

This directory contains human-readable official source metadata, five quick Saudi location presets, and an explicitly labeled deterministic Riyadh demo scenario. Production location choices come from validated Saudi geocoding results; the demo fixture is returned through an internal service without network calls.
